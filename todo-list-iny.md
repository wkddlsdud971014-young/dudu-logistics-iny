# todo-list — 두두택배 데이터 정리 (2026-08-06)

## 잊으면 안 되는 핵심

1. **줄(행)은 지우지 않는다.** 표시 칸으로 분류만 한다.
2. **고치지 않고, 답도 정하지 않는다.** 문제를 찾고 "왜 생겼을까"까지만 적는다. 해결은 내일 의논.
3. 오늘의 1차 목표는 **검산**(정답 파일과 대조), 2차 목표는 **내일 키오스크 화면의 재료** 만들기.

---

## | 3 | 처리 방침 — 무엇을 어떤 방식으로 처리했나

> 항목마다 처리 방식이 다르고, 그래서 "못 한 것"의 이유도 다 다르다. 아래에 구분해서 적음.

### [V] 한 일 1. 중복 운송장 — 처리 방식: **표시만 함**

- 같은 운송장 번호(tracking_no)가 두 번씩 45건(90줄). 25칸 전부 똑같았다.
- 표시 칸(dup_flag)에 (중복-첫행 / 중복-재등장)으로 적어둠.
- **못 한 것**: 지우기
- **못 한 이유**: 같은 번호가 서로 다른 두 물건에 붙었을 가능성이 있다.
  배송 기록(tracking_events.csv)에서 이동 기록이 한 세트인지 두 세트인지 봐야 확정된다.
- **왜 생겼을까(추측)**: 시스템이 같은 접수를 두 번 저장했거나, 번호 발급이 겹쳤거나.

### [V] 한 일 2. 글자 표기 — 처리 방식: **표준화함 (뜻이 100% 확실한 것만)**

- 440칸 통일. 바꾼 내용은 기록 칸(fix_note)에 남김.
  - 배송 상태(status): (배송 완료 / 배달완료 / 완료) → (배송완료), (미 배송 / 미배달) → (미배송)
  - 크기 등급(size_grade): (중 → 중형) (소 → 소형) (대 → 대형) (극소 → 극소형)
  - 접수 경로(channel): (편의접수 / 편의점 접수) → (편의점접수), (인터넷 / 온라인) → (인터넷접수)
  - 도착 지역(receiver_area): (서울시 / 서울특별시 / SEOUL) → (서울) 등
- **표준화 못 한 것과 각각의 이유** (보류 칸 hold_flag에 표시, 107줄):
  - (배송실패)(반송)(회수)(배송중)(배달중) → **뜻을 몰라서.** 미배송·반품과 같은 말인지
    다른 상태인지 현장에 물어봐야 안다. 같다고 합치면 다른 상태를 지워버릴 수 있다.
  - (초과)(규격외) → **문서에 없는 값이라서.** 오타가 아니라 실제로 쓰는 등급일 수
    있다. 접수 규정(02_접수규정.md)에서 등급 체계를 확인해야 한다.
  - 지역칸의 (도서산간) 19줄 → **원래 지역을 몰라서.** 지역 이름 자리에 권역 이름이
    들어왔다. 동네(receiver_dong) 이름으로 6줄은 지역 추정이 가능했고, 나머지 13줄은
    원래 주소 기록이 있어야 복원된다.
- **왜 생겼을까(추측)**: 지역·상태·등급을 손으로 입력하게 해서 사람마다 부르는 말이 달랐던 듯.

### [V] 한 일 3. 부피무게·요금무게 — 처리 방식: **검산함 (계산값을 별도 칸에, 원본 안 고침)**

- 부피무게: 가로(width_cm)×세로(height_cm)×높이(depth_cm)÷6000 으로 재계산
  → 계산 칸(calc_volume_weight_kg), 판정 칸(vw_check)
- 요금무게: 실제무게(weight_kg)와 부피무게 중 큰 값 → (calc_billed_weight_kg), (bw_check)
- 알게 된 것: 치수가 정상인 2,903줄은 부피무게 전부 일치. 문제는 부피무게가 아니라
  치수 칸에 이상값(9999, 음수)이 든 56줄이었다. / 요금무게가 규칙과 다른 62줄 발견.
- **검산했지만 확정 못 한 것과 이유**:
  - 이상 치수 56줄의 진짜 치수 → **원본 기록이 없어서.** 상자를 다시 잴 수 없다.
  - 요금무게 62줄이 요금 오류로 이어졌는지 → **요금표가 없어서.** 규정 필요.
- **왜 생겼을까(추측)**: 치수 입력에 제한이 없어서 오타(9999)나 임시값(-10)이 그대로 저장된 듯.

### [V] 한 일 4. 도착 예정일(eta_date) — 처리 방식: **검산함 (일수 계산해서 판정만)**

- 접수→도착예정 일수를 계산 칸(calc_eta_days)에, 판정을 (eta_check)에 넣음.
- 정상범위 2,850줄 (일반 1~3일, 제주 2~4일, 도서산간 3~5일) / 형식만 이상 89줄 / 빈칸 70줄
- **의심으로 분류한 것**: 제주·도서산간인데 다음날 도착으로 적힌 36줄.
  배·비행기를 타야 하는 지역이라 하루 만에 못 간다.
- **확정 못 한 이유**: 권역별로 며칠이 걸리는 게 규정인지 몰라서. 규정이 오면 확정.
- **왜 생겼을까(추측)**: 예정일을 자동 계산이 아니라 사람이 적는 것 같다. 권역을 안 보고
  일반 지역 기준(다음날)으로 적은 듯.

### [V] 한 일 5. 표 안에서 앞뒤 안 맞는 것 — 처리 방식: **분류만 함 (수정 0)**

- 새 칸(pattern_check)에 태그로 분류. 태그 붙은 줄: 1,205줄.
  - (전화-형식이상) 345줄 — 하이픈 없이 숫자만 적힘. **왜?** 숫자만 입력받은 듯
  - (접수일시-형식이상) 289줄 — 날짜 적는 모양이 제각각. **왜?** 자유 입력인 듯
  - (전화-빈칸) 265줄 — 미배송이면 연락을 못 하는 실무 문제로 이어짐
  - (요금-빈칸) 215줄
  - (이름에-메모붙음) 100줄 — "(경비실)" "(직접수령)" 같은 배송 메모가 이름 칸에 들어옴.
    **왜?** 메모 쓸 칸이 따로 없었던 듯
  - (미완료인데-도착시각있음) 70줄 / (완료인데-도착시각없음) 65줄 — 상태와 시각이 어긋남
  - (요금-같은조건다수와다름) 49줄 — 같은 등급·같은 권역이면 대부분 같은 요금인데
    (소형-일반 4,000 / 중형-일반 6,000 / 대형-일반 9,000 / 중형-제주 9,000) 혼자 다른 값.
    **왜?** 요금을 수기로 계산했거나 할인·할증이 기록 안 된 듯
  - (시간역전-도착이접수보다빠름) 39줄 — 물리적으로 불가능.
    **왜?** 둘 중 한쪽 시각이 잘못 기록된 것. 어느 쪽인지는 배송 기록이 있어야 안다
- **깨끗했던 것 (검사했는데 문제 0건)**:
  - 같은 고객번호(sender_id)에 이름(sender_name)이 다른 경우 → 0건
  - 동네(receiver_dong)와 지역(receiver_area)이 안 맞는 경우 → 0건
  - 운송장 앞 2자리와 지점 코드(branch_code) 불일치 → 0건

### [ ] 남은 일. 고객 명단과 맞춰보기 — 아직 못 함 (검산이 먼저라 뒤로 미룸)

- customers.csv(1,200행)는 data/ 폴더에 있다. 파일이 없어서가 아니라 시간 순서상 뒤로 밀린 것.
- 보내는 분 번호(sender_id)가 명단에 있는 번호인지, 이름이 명단과 같은지.
- 파일 안에서의 자기모순은 0건이었으니, 명단과 대조해야만 잡히는 오류가 남은 것.

---

## | 5 | 검산 — 만든 것을 정답과 대조했나 ← 오늘의 1차 목표

### [V] verified_300.csv(정답 299건)와 대조 — 완료. 결과는 `result.md`

- 스텝 1: 정답과 내 정리본(data/shipments-update.csv)을 운송장 번호(tracking_no)로 연결
- 스텝 2: 칸마다 값이 같은지 비교해 점수 내기
- 스텝 3: 다른 칸들을 모아 "오류 유형 목록" 만들기 → 다음 주 AI(LLM) 재료
- 결과: 299건 전부 생존(사라짐 0). 24칸 대조에서 266칸 불일치
- 전략이 맞았던 것: 표기 통일(receiver_area 100% 일치), 요금무게 검산, 이상값 추정 안 하고 보류
- 보류가 감점된 것: 전화번호 형식(79.3%), 접수일시 형식(92.0%), 부피무게를 원래 칸에 안 채운 것
- 파일 안에서는 못 잡는 유형 확인: sender_id 12건 → customers.csv 대조가 실제로 필요했다

---

## | 6 | 남기기 — 내일의 내가 이어받을 수 있나

- 결과 파일: **data/shipments-update.csv** 한 개 (원래 25칸 + 만든 칸 10개)
  - (dup_flag) 중복 / (fix_note) 바꾼 기록 / (hold_flag) 보류
  - (calc_volume_weight_kg)(vw_check) 부피무게 검산
  - (calc_billed_weight_kg)(bw_check) 요금무게 검산
  - (calc_eta_days)(eta_check) 예정일 검산
  - (pattern_check) 앞뒤 안 맞는 것 분류 태그
- 숫자: 전체 3,045줄 (삭제 0) / 표준화 440칸 / 보류 107줄 / 검산 의심 62+36+49줄
  / 분류 태그 1,205줄 / 깨끗 확인 3항목

---

## | 4 | 최소 기능(내일) — 문제 패턴 목록 (답은 내일 같이 정한다)

> 해결책이 아니라 "이런 패턴이 있더라"까지만. 기능은 내일 의논해서 결정.

1. 지역 이름이 (서울/서울시/서울특별시/SEOUL) 4갈래 — 자유 입력에서 생긴 패턴
2. 상태·등급·경로도 같은 값이 여러 표기 — 부르는 말이 사람마다 다른 패턴
3. 무게·치수에 (11.6kg)(32cm)처럼 단위가 붙음 — 손으로 친 흔적
4. 전화번호 하이픈 누락 345줄 — 숫자만 입력된 패턴
5. 치수에 (9999)(음수) — 입력 제한이 없어서 오타·임시값이 저장된 패턴
6. 이름 칸에 배송 메모 — 메모 쓸 자리가 없어서 생긴 패턴
7. 제주·도서산간 예정일이 다음날 — 권역을 반영 안 하고 적은 패턴
8. 같은 조건인데 요금이 다른 49줄 — 수기 계산 또는 미기록 할인의 패턴
9. 예외 물품 (초과)(규격외) — 선택지에 없는 물품이 들어온 패턴
