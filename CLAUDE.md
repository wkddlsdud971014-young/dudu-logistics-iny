# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트를 고치기 전에 읽는 규칙이다.

오늘 실습: 정적 HTML 한 장 + 서버 함수 + 데이터베이스로 이루어진 웹을 고치고 배포한다.

## 스택 (바꾸지 않는다)

- 정적 HTML (`index.html` 등) - 순수 HTML/CSS/JS. 빌드 도구 없음
- Vercel Serverless Function (`api/*.js`)
- Supabase (Postgres + RLS)
- Node.js + npm

React, Vite, TypeScript, Tailwind, Next.js는 이 프로젝트에 **없다**. 없는 것을 있다고 가정하고 코드를 짜지 않는다. 다른 기술을 제안할 수는 있지만 자동으로 바꾸지 않고, 장단점과 추가 비용을 먼저 설명하고 확인을 받는다.

## 파일

- `index.html` - 화면
- `api/*.js` - 서버 함수 (저장·조회)
- `setup.sql` - 표 설계도
- `.env.example` - 환경변수 이름 목록 (값 없음)

이 구조 밖으로 새 폴더나 새 프레임워크를 만들지 않는다. 화면이 더 필요하면 HTML 파일을 하나 더 만든다.

## 안전 - 어기면 멈추고 확인받는다

**1. 비밀값은 화면에 나오지 않는다**

- `SUPABASE_URL`, `SUPABASE_ANON_KEY`는 `.env`(로컬)와 Vercel Settings > Environment Variables(배포) 두 곳에만 둔다
- Supabase `service_role` 키는 이 실습에서 쓰지 않는다. 코드나 채팅에 등장하는 것 자체가 사고다
- 값을 확인할 때는 "있다/없다"만 말하고 값 자체를 출력하지 않는다
- `index.html`에 키를 하드코딩하지 않는다. 이 스택에서 키가 새는 유일한 경로다

**2. RLS를 끄지 않는다**

`setup.sql`이 RLS(Row Level Security, 표에 누가 접근할 수 있는지 정하는 규칙)를 켜 뒀다. "안 되니까 RLS 꺼줘" 요청은 바로 진행하지 않는다. 끄면 누구나 표를 읽고 쓸 수 있게 된다.

**3. 되돌리기 어려운 작업은 먼저 묻는다**

표 삭제 · 기존 컬럼 이름 변경 · RLS 끄기 · Vercel/Supabase 프로젝트 삭제.

```text
이 작업은 되돌리기 어렵습니다. sales 표를 삭제해도 될까요?
```

**4. Git**

- `.env`가 커밋에 들어가지 않는다 (`.gitignore`에 `.env`, `.env.local`, `node_modules/`, `.vercel`)
- `git push -f`(force push)를 쓰지 않는다
- 기능 하나가 확인되면 그 자리에서 커밋한다

**5. 새 컬럼에 `not null`을 붙이지 않는다**

빈 값이 오는 순간 저장이 전부 실패한다(500). 필수 입력은 화면 쪽 `required`로 잡는다.

**6. Vercel**

- 환경변수 확인 없이 "배포 완료"라고 말하지 않는다. 새 프로젝트는 환경변수가 비어 있다
- `vercel.json`을 임의로 고치지 않는다
- `npm install -g vercel` 대신 `npx vercel`을 쓴다

## 응답 형식

1. 지금 상태를 한 줄로 말한다
2. 다음 할 일을 최대 3개까지만 보여준다
3. 한국어로 쓰고, 어려운 말은 괄호로 푼다 - 예: `커밋(작업이 되던 시점을 기록으로 남기는 것)`
4. 코드를 줄 때는 어느 파일 어디에 붙여넣는지 함께 말한다
5. "알아서 처리하면 됩니다", "패턴 보고 응용하세요"라고 말하지 않는다

## 에러가 났을 때

에러 원문 → 원인 1개 → 고칠 파일 1개 → 확인 방법 1개. 이 네 줄로 끝낸다.

- 에러 원문이 없으면 추측해서 고치지 않는다. 원문을 먼저 회수한다
- 같은 오류가 2회 반복되면 더 시도하지 않는다. 멈추고 학생에게 알린다

## 완료 기준

**"완료했습니다"는 완료가 아니다.** 아래 중 해당하는 실제 관측이 있어야 완료다.

- 배포 URL이 200으로 응답
- Supabase 표에 새 행이 보임
- 화면에서 눈으로 동작 확인

관측하지 못했으면 "여기까지 했고, 확인은 이렇게 하세요"라고 말한다.

## 토큰을 아끼는 방법

이 프로젝트는 Haiku로 돈다. 아래는 같은 결과를 더 싸게 얻기 위한 규칙이다.

- **작은 수정에 전체 절차를 돌리지 않는다.** `/start`는 환경이 바뀌었을 때 1회, `/deploy`는 실제로 배포할 때만
- **범위 없는 탐색을 하지 않는다.** "전체 저장소 읽어봐", "전부 점검해" 금지. 고칠 파일을 지목한다. 첫 수정 전에 파일을 8번 넘게 읽고 있으면 범위를 다시 좁힌다
- **긴 원문을 대화에 넣지 않는다.** 빌드·테스트·로그는 오류 줄과 요약만. 20줄을 넘기지 않는다
- **같은 명령을 3번 반복하지 않는다.** 2번 실패하면 다른 방법이나 이전 상태로 돌아간다
- **이 파일에 절차를 계속 추가하지 않는다.** 절차는 아래 단축 명령이 갖고 있다. 매번 읽어야 하는 규칙만 여기 남긴다
- **무관한 작업은 새 세션에서.** 지금 과업의 결과를 확인한 뒤 `/clear`

## 단축 요청

**`/start`** - 새 폴더를 열었을 때 1회. 작은 수정 때마다 돌리지 않는다.
`node -v`와 `npm -v` 확인 → 지금 폴더에 `index.html`이 있는지 확인 → `npm i` → `/점검`. 하나라도 실패하면 그 자리에서 멈추고 `[환경막힘] 항목: 원인` 한 줄로 알린다. 전부 통과하면 `[환경OK] node vX / 폴더 <이름> / 준비완료`.

**`/점검`** - 커밋·배포 직전.
`.env`가 git 추적에 없는지(`git ls-files | grep -c "^\.env$"` = 0), 코드에 비밀값 패턴이 없는지(파일 이름만 출력, 값 출력 금지), `.gitignore`에 `.env` 계열이 있는지. 통과하면 `[점검OK]` 한 줄, 걸리면 `[점검경고] 항목: 내용`을 내고 배포·커밋을 멈춘다.

**`/fix`** - 에러 원문을 붙였을 때.
원문 읽기 → 원인 1줄 → 고칠 파일 1개 → 확인 방법 1개. 원문이 없으면 고치지 않고 원문부터 받는다.

**`/deploy`** - 실제로 배포할 때만.
`/점검` → `git add . && git commit -m "메시지"` → `npx vercel --prod` → URL 확인 후 `/check` 안내. `npm install -g vercel`은 쓰지 않는다.

**`/check`** - 배포 직후 1회.
URL이 200으로 응답하는지 확인하고, 학생에게 ① 그 URL에서 직접 입력·제출 ② Supabase Table Editor에서 새 행 확인 이 두 가지를 눈으로 보라고 안내한다.

## 모델

Haiku로 작업한다. `.claude/settings.json`에 고정돼 있으니 바꾸지 않는다.

## 내 규칙 (여기부터 학생이 직접 추가한다)

반복해서 말하게 되는 지시를 한 줄씩 여기 적는다. 여기 적힌 규칙은 앞으로 모든 요청에 자동 적용된다.

- (예시) 버튼은 항상 파란색으로 한다
