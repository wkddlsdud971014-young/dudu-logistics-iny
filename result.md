## 검산

**정답 세트 생존**

- 299건 중 299건 남음 / 0건 사라짐
- 사라진 운송장: 없음 (행을 하나도 지우지 않는 방침이라 전부 생존)

**칸 - `receiver_phone`**

- 처리 후: 62건 다름 / 0건 사라짐 (일치율 79.3%, 분모 299)
- 예상과 달랐던 것: 62건 중 30건은 원래 빈칸(복원 불가라 보류한 것). 나머지는 하이픈 누락에
  더해 공백 구분(`010 6948 5817`)이라는 변형도 있었다. 형식 복원은 안전한 수정이었는데
  보류했더니 그대로 감점됐다

**칸 - `item_name`**

- 처리 후: 33건 다름 / 0건 사라짐 (일치율 89.0%, 분모 299)
- 예상과 달랐던 것: "고객이 적는 값이라 검증 불가"로 다루지 않기로 했던 칸. 그런데 띄어쓰기
  (`김 치`)뿐 아니라 아예 다른 물품(`세제`를 `냉동식품`으로)도 있었다. 감사팀은 전화로
  확인해 정정했다 - 검증 불가가 아니라 검증 수단이 달랐던 것

**칸 - `price`**

- 처리 후: 28건 다름 / 0건 사라짐 (일치율 90.6%, 분모 299)
- 예상과 달랐던 것: 28건 중 21건이 빈칸. 요금표 없이 못 채운다는 보류 판단 자체는 맞았고,
  정답은 자동 계산으로 채워져 있었다

**칸 - `delivered_at`**

- 처리 후: 28건 다름 / 0건 사라짐 (일치율 90.6%, 분모 299)
- 예상과 달랐던 것: 형식 차이(`9:28` vs `09:28`), 빈칸에 더해 시각 자체가 다른 건도 있었다.
  시간 역전 의심과 같은 뿌리로 보임

**칸 - `accepted_at`**

- 처리 후: 24건 다름 / 0건 사라짐 (일치율 92.0%, 분모 299)
- 예상과 달랐던 것: 보류했던 비표준 형식이 전부 감점됐다. 월/일이 모호한 건만 보류하고
  나머지는 표준형으로 통일했어도 됐다

**칸 - `eta_date`**

- 처리 후: 14건 다름 / 0건 사라짐 (일치율 95.3%, 분모 299)
- 예상과 달랐던 것: 점 표기(`2026.07.07`) 12건 + 빈칸 2건. 날짜 값 자체는 맞았다

**칸 - `receiver_name`**

- 처리 후: 12건 다름 / 0건 사라짐 (일치율 96.0%, 분모 299)
- 예상과 달랐던 것: 정답은 이름만 있다. 괄호 메모와 `님` 호칭을 분리하는 게 정답이었다 -
  "별도 칸으로 분리" 추천이 맞는 방향

**칸 - `sender_id`**

- 처리 후: 12건 다름 / 0건 사라짐 (일치율 96.0%, 분모 299)
- 예상과 달랐던 것: 파일 안 자기모순(같은 번호 다른 이름)은 0건이었는데, 정답과 대조하니
  ID 자체가 틀린 12건이 나왔다. 고객 명단(customers.csv) 없이는 못 잡는 유형이 실재했다

**칸 - `size_grade`**

- 처리 후: 12건 다름 / 0건 사라짐 (일치율 96.0%, 분모 299)
- 예상과 달랐던 것: 빈칸 8건 + 등급이 아예 틀린 4건. 등급표만 있으면 요금무게에서
  자동 계산으로 채우는 게 답이었다

**칸 - `volume_weight_kg`**

- 처리 후: 11건 다름 / 0건 사라짐 (일치율 96.3%, 분모 299)
- 예상과 달랐던 것: 11건 전부 원래 칸의 빈칸. 우리는 계산값을 참고 칸(calc)에만 두고
  원래 칸은 비워 뒀는데, 정답은 원래 칸에 채워져 있었다. 계산 근거가 확실한 값은
  원래 칸을 채우는 게 정답이었다

**칸 - `status`**

- 처리 후: 9건 다름 / 0건 사라짐 (일치율 97.0%, 분모 299)
- 예상과 달랐던 것: 보류했던 값의 답이 일부 나왔다 - `반송`의 정답은 `반품`, `배송중`의
  정답은 `배송출발`. 다만 `미배송`·`반품`이 실제로는 `배송완료`인 건도 있어 - 표기 문제가
  아니라 상태 갱신 누락도 섞여 있다

**칸 - `billed_weight_kg`**

- 처리 후: 6건 다름 / 0건 사라짐 (일치율 98.0%, 분모 299)
- 예상과 달랐던 것: 부피무게를 반영 안 하고 실제무게를 그대로 적은 건들(1.7 → 정답 3.3).
  max(실제, 부피) 검산으로 잡아낸 62건 의심과 같은 유형 - 검산 방향이 맞았다

**칸 - `channel`**

- 처리 후: 6건 다름 / 0건 사라짐 (일치율 98.0%, 분모 299)
- 예상과 달랐던 것: 표기 통일로는 못 잡는 유형 - 표기가 아니라 경로 값 자체가 잘못 기록됨
  (편의점접수를 방문접수로). 접수 기기가 자동 기록해야 하는 이유가 하나 더 생겼다

**칸 - `weight_kg`**

- 처리 후: 4건 다름 / 0건 사라짐 (일치율 98.7%, 분모 299)
- 예상과 달랐던 것: 999와 -1로 보류했던 이상값의 실제 무게가 정답에 있었다(999 → 7.9).
  추정해서 채우지 않은 게 맞았고, 정답 원천이 있으면 복원되는 유형

**칸 - `region_type`**

- 처리 후: 2건 다름 / 0건 사라짐 (일치율 99.3%, 분모 299)
- 예상과 달랐던 것: 권역 오분류(일반을 제주로, 제주를 도서산간으로). 지역에서 자동
  도출해야 하는 이유

**칸 - `height_cm`**

- 처리 후: 2건 다름 / 0건 사라짐 (일치율 99.3%, 분모 299)
- 예상과 달랐던 것: `0`(미입력)과 `3700`(37의 자릿수 오타). 범위 제한으로 막히는 유형

**칸 - `depth_cm`**

- 처리 후: 1건 다름 / 0건 사라짐 (일치율 99.7%, 분모 299)
- 예상과 달랐던 것: `0` 한 건

**다름 0 / 사라짐 0 (일치율 100%)**: `sender_name` `branch_code` `branch_name`
`receiver_area` `receiver_dong` `width_cm` `category` - 특히 `receiver_area`는
통일 245칸을 거치고도 정답과 완전 일치. 표기 통일 전략이 검증됐다