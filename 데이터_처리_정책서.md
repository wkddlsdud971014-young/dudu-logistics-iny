# 두두 로지스틱스 - 데이터 처리 정책서

- **작성자**: 장인영
- **작성일**: 2026-08-06
- **대상**: 접수 원장 `shipments` 3,045건 / 25칸

---

## 오늘 내는 것 세 가지

1. **정리 방식** - 어떤 기준으로 처리했는가
2. **오류 분류** - 어떤 예외가 나왔는가
3. **처리 방안** - 다음 단계에서 어떻게 할 것인가

---

## 조사 현황

- [x] `tracking_no` - 운송장 번호
- [x] `accepted_at` - 접수 시각
- [x] `branch_code` - 지점 코드
- [x] `branch_name` - 지점 이름
- [x] `sender_id` - 보내는 사람 ID
- [x] `sender_name` - 보내는 사람 이름
- [x] `receiver_name` - 받는 사람 이름
- [x] `receiver_phone` - 받는 사람 전화
- [x] `receiver_area` - 받는 지역
- [x] `receiver_dong` - 받는 동
- [x] `region_type` - 권역 구분
- [x] `category` - 물품 분류
- [ ] `item_name` - 물품명 (일부러 안 봄 - "다루지 않기로 한 것" 참고)
- [x] `weight_kg` - 무게
- [x] `width_cm` - 가로
- [x] `height_cm` - 세로
- [x] `depth_cm` - 높이
- [x] `volume_weight_kg` - 부피 무게
- [x] `billed_weight_kg` - 청구 무게
- [x] `size_grade` - 크기 등급
- [x] `price` - 요금
- [x] `eta_date` - 도착 예정일
- [x] `status` - 배송 상태
- [x] `delivered_at` - 배송 완료 시각
- [x] `channel` - 접수 경로

문제 없음을 확인한 칸: `branch_code`/`branch_name`(운송장 앞 2자리와 100% 일치, 6개 지점),
`sender_id`/`sender_name`(같은 번호에 다른 이름 0건), `receiver_dong`(지역과 모순 0건),
`region_type`(3종), `category`(8종). **검사했는데 깨끗한 것도 기록해 둠.**

---

# 칸별 정책

---

## 칸 - `tracking_no` (운송장 번호)

### 1. 초기 상태

- 값 종류: 고유 번호 3,000개 / 전체 3,045행
- 빈 칸: 0건
- 형식: 숫자 10자리 (전부 자릿수 일치)
- 어떻게 셌나: 파이썬으로 고유값 개수와 전체 행 수 비교

### 2. 예외

**예외 1 - 같은 번호가 두 번씩 등장**

- 몇 건: 45개 번호, 90행
- 어떻게 찾았나: 번호별로 세어서 2회 이상인 것을 골라냄. 두 행을 25칸 전부 비교하니 45쌍 모두 내용까지 완전히 동일

### 3. 처리

**예외 1 - 중복 번호**

- 분류: 보류 (새 칸 `dup_flag`에 중복-첫행 / 중복-재등장으로 표시만)
- 왜 이렇게 정했나: 내용이 같아 보여도, 같은 번호가 서로 다른 두 물건에 발급된 사고일 수 있다. 그 경우 한 행을 지우면 실제 물건 하나가 장부에서 사라진다
- 왜 생겼다고 보는가: 시스템이 같은 접수를 두 번 저장했거나, 번호 발급이 겹쳤거나. 특정 지점·특정 날짜에 몰려 있지 않고 6개 지점 석 달에 고르게 퍼져 있어 일회성 장애는 아닌 듯
- 확신도: 추천 (배송 추적 기록 `tracking_events.csv`에서 이동 기록이 한 세트인지 두 세트인지 보면 확정 가능)

### 4. 앞으로 어디서 막나

**예외 1 - 중복 번호**

- 층: 시스템
- 어떻게: 번호는 사람이 만들지 않고 시스템이 발급한다. 이미 있는 번호는 저장 자체를 거부한다

### 5. 못 정한 것 - 누구에게 물어야 하나

- 이중 저장인지 번호 발급 사고인지 → 택배 시스템 관리자 (tracking_events.csv 대조로도 판별 가능)

---

## 칸 - `status` (배송 상태)

### 1. 초기 상태

- 값 종류: 16가지 (문서상 7가지여야 함)
- 빈 칸: 0건
- 형식: 글자, 자유 입력으로 보임
- 어떻게 셌나: 값별로 몇 건인지 전부 세어서 문서의 7종과 대조

### 2. 예외

**예외 1 - 같은 뜻을 다르게 적음 (`배송 완료`/`배달완료`/`완료`, `미 배송`/`미배달`)**

- 몇 건: 46건
- 어떻게 찾았나: 값 목록을 놓고 띄어쓰기·동의어 변형을 눈으로 대조

**예외 2 - 문서에 없는 상태 (`배송실패` `반송` `회수` `배송중` `배달중`)**

- 몇 건: 82건 (14+21+13+18+16)
- 어떻게 찾았나: 문서의 7종 목록에 없는 값을 골라냄

### 3. 처리

**예외 1 - 표기 변형**

- 분류: 통일 (46칸, 바꾼 내용은 `fix_note` 칸에 원값 기록)
- 왜 이렇게 정했나: 배달=배송 동의어, 띄어쓰기 차이는 뜻이 100% 같다. 통일 안 하면 "미배송 몇 건" 집계 자체가 틀린다
- 왜 생겼다고 보는가: 상태를 손으로 입력해서 사람·지점마다 부르는 말이 달랐던 듯
- 확신도: 확정

**예외 2 - 문서에 없는 상태**

- 분류: 보류 (`hold_flag`에 표시, 값은 그대로)
- 왜 이렇게 정했나: `배송실패`가 미배송과 같은 말인지, `회수`가 반품과 다른 별도 절차인지 모른다. 같다고 합쳐 버리면 실제로는 다른 상태를 지워버릴 수 있다. "문서보다 데이터가 먼저"
- 왜 생겼다고 보는가: 문서가 오래됐거나, 현장에서 상태를 세분해 쓰고 있거나
- 확신도: 선택지 (같은 뜻이면 통일 / 다른 뜻이면 상태 체계에 추가 - 현장 답을 듣고 고르면 됨)

### 4. 앞으로 어디서 막나

**예외 1 - 표기 변형**

- 층: 시스템
- 어떻게: 상태는 입력하지 않는다. 바코드 스캔 등 이벤트가 생기면 시스템이 자동으로 바꾼다

**예외 2 - 문서에 없는 상태**

- 층: 정책
- 어떻게: 상태 체계를 몇 가지로 할지(배송실패·회수를 별도 상태로 둘지) 규칙부터 정해야 한다

### 5. 못 정한 것 - 누구에게

- 5가지 값의 정확한 뜻 → 현장(지점 직원, 배송 기사)

---

## 칸 - `size_grade` (크기 등급)

### 1. 초기 상태

- 값 종류: 10가지 + 빈 칸 (문서상 4가지여야 함)
- 빈 칸: 59건
- 형식: 글자
- 어떻게 셌나: 값별 건수를 세어 문서의 4등급과 대조

### 2. 예외

**예외 1 - 축약 표기 (`중` `소` `대` `극소`)**

- 몇 건: 34건
- 어떻게 찾았나: 값 목록에서 4등급의 줄임말 형태를 대조

**예외 2 - 문서에 없는 등급 (`초과` `규격외`)**

- 몇 건: 6건
- 어떻게 찾았나: 4등급 목록에 없는 값 골라냄

**예외 3 - 빈 칸**

- 몇 건: 59건

### 3. 처리

**예외 1 - 축약 표기**

- 분류: 통일 (34칸, `fix_note`에 기록)
- 왜 이렇게 정했나: `중`이 중형의 줄임말인 것은 명백
- 왜 생겼다고 보는가: 손으로 입력하다 줄여 쓴 것
- 확신도: 확정

**예외 2 - 문서에 없는 등급**

- 분류: 보류 (`hold_flag`)
- 왜 이렇게 정했나: 오타가 아니라 4등급에 안 들어가는 대형 화물을 실제로 그렇게 부르는 것일 수 있다
- 왜 생겼다고 보는가: 등급 기준을 넘는 물건이 실제로 접수됐는데 선택지가 없었던 것
- 확신도: 추천 (접수 규정의 등급표를 보면 확정 가능)

**예외 3 - 빈 칸**

- 분류: 보류
- 왜 이렇게 정했나: 등급은 요금무게에서 계산되는 값이라 채울 수는 있지만, 등급 경계표(규정)가 없어 아직 계산 근거가 없다
- 확신도: 추천 (규정 오면 자동 계산으로 채우기)

### 4. 앞으로 어디서 막나

**예외 1·3 - 축약·빈 칸**

- 층: 시스템
- 어떻게: 등급은 입력받지 않는다. 무게·치수에서 자동 계산한다

**예외 2 - 규격 밖 화물**

- 층: 정책 + 운영
- 어떻게: 4등급 밖 화물을 받을지부터 정책으로 정하고, 받는다면 키오스크에서는 접수 불가 안내 후 상담원 연결로 뺀다

### 5. 못 정한 것 - 누구에게

- `초과`/`규격외`가 공식 등급인지 → 택배 회사(규정 담당)

---

## 칸 - `channel` (접수 경로)

### 1. 초기 상태

- 값 종류: 10가지 + 빈 칸 (문서상 4가지)
- 빈 칸: 18건
- 형식: 글자
- 어떻게 셌나: 값별 건수 대조

### 2. 예외

**예외 1 - 표기 변형 (`편의접수`/`편의점 접수`, `인터넷`/`온라인`, `대리점접수`, `방문`)**

- 몇 건: 115건
- 어떻게 찾았나: 4개 경로의 변형 형태를 목록에서 대조

**예외 2 - 빈 칸**

- 몇 건: 18건

### 3. 처리

**예외 1 - 표기 변형**

- 분류: 통일 (115칸, `fix_note`에 기록)
- 왜 이렇게 정했나: 경로가 4개뿐이라 `온라인`이 가리킬 수 있는 것은 인터넷접수뿐
- 왜 생겼다고 보는가: 자유 입력
- 확신도: 확정

**예외 2 - 빈 칸**

- 분류: 보류
- 왜 이렇게 정했나: 어느 경로인지 추정할 근거가 행 안에 없다
- 확신도: 확정 (보류가 맞다)

### 4. 앞으로 어디서 막나

- 층: 시스템
- 어떻게: 접수 경로는 사람이 적지 않는다. 접수한 기기(키오스크/웹/창구)가 자동으로 기록한다

### 5. 못 정한 것 - 누구에게

- 없음

---

## 칸 - `receiver_area` (받는 지역)

### 1. 초기 상태

- 값 종류: 25가지 (통일 후 18가지)
- 빈 칸: 0건
- 형식: 글자, 자유 입력으로 보임
- 어떻게 셌나: 값별 건수 대조

### 2. 예외

**예외 1 - 같은 지역 다른 표기 (`서울`/`서울시`/`서울특별시`/`SEOUL`, `대전시`, `울산광역시` 등)**

- 몇 건: 245건
- 어떻게 찾았나: 값 목록에서 같은 시·도의 변형끼리 묶음

**예외 2 - 지역 자리에 권역 이름 (`도서산간`)**

- 몇 건: 19건
- 어떻게 찾았나: 시·도 이름이 아닌 값 골라냄

### 3. 처리

**예외 1 - 표기 갈림**

- 분류: 통일 (245칸, `fix_note`에 기록)
- 왜 이렇게 정했나: 서울이 네 덩어리로 갈리면 지역별 집계가 안 된다. 시·도 표준명으로 묶는 것은 뜻이 안 변한다
- 왜 생겼다고 보는가: 자유 입력이라 사람마다 다르게 적음
- 확신도: 확정

**예외 2 - `도서산간` 19건**

- 분류: 보류 (`hold_flag`)
- 왜 이렇게 정했나: 어느 섬·산간인지 원래 주소를 모른다. 동네 칸(`receiver_dong`)으로 6건은 지역 추정이 가능했지만 나머지 13건은 근거가 없다
- 왜 생겼다고 보는가: 지역을 적는 자리에 권역 구분을 적어버린 입력 실수
- 확신도: 추천 (원래 주소 기록이 있으면 복원 가능)

### 4. 앞으로 어디서 막나

**예외 1 - 표기 갈림**

- 층: 시스템
- 어떻게: 자유 입력을 없애고 선택 목록(또는 주소 검색)으로 받는다

**예외 2 - 권역 이름 혼입**

- 층: 시스템
- 어떻게: 권역(`region_type`)은 지역에서 자동 계산하고, 사람이 적는 칸에서 없앤다

### 5. 못 정한 것 - 누구에게

- 표준 목록의 단위(광역시도로 할지 배송 권역으로 할지) → 요금 규정과 붙어 있어 택배 회사와 협의

---

## 칸 - `receiver_phone` (받는 사람 전화)

### 1. 초기 상태

- 값 종류: 대부분 010-XXXX-XXXX, 일부는 숫자만
- 빈 칸: 265건
- 형식: 글자(숫자+하이픈 섞임)
- 어떻게 셌나: 010-0000-0000 모양인지 형식 검사

### 2. 예외

**예외 1 - 하이픈 누락 (`1033478638` 같은 숫자만)**

- 몇 건: 345건
- 어떻게 찾았나: 형식 검사에서 어긋난 값

**예외 2 - 빈 칸**

- 몇 건: 265건
- 어떻게 찾았나: 빈 값 필터

### 3. 처리

**예외 1 - 하이픈 누락**

- 분류: 보류 (`pattern_check`에 태그만. 복원은 안 함)
- 왜 이렇게 정했나: 하이픈 복원 자체는 안전하지만, 자릿수가 이상한 번호도 섞여 있어 일괄 복원 전에 자릿수 검사가 먼저다
- 왜 생겼다고 보는가: 숫자만 입력받는 화면이었던 듯
- 확신도: 추천 (10~11자리 확인 후 복원하면 됨)

**예외 2 - 빈 칸**

- 분류: 보류
- 왜 이렇게 정했나: 지어낼 수 없는 값. 특히 미배송 건에서 전화가 없으면 연락을 못 해 물건이 쌓인다 - 데이터 문제가 아니라 운영 문제로 이어짐
- 확신도: 확정 (보류가 맞다)

### 4. 앞으로 어디서 막나

**예외 1 - 형식**

- 층: 시스템
- 어떻게: 입력하면 자동으로 010-0000-0000 모양이 되게 하고, 자릿수가 안 맞으면 다음으로 못 넘어가게 한다

**예외 2 - 빈 칸**

- 층: 시스템 + 운영
- 어떻게: 필수 입력으로 막되, 번호를 모르는 접수(예: 관공서 발송)를 허용할지는 운영에서 정한다

### 5. 못 정한 것 - 누구에게

- 전화 없는 접수를 받아줄지 → 접수 창구 운영 담당

---

## 칸 - `receiver_name` (받는 사람 이름)

### 1. 초기 상태

- 값 종류: 자유 글자
- 빈 칸: 0건
- 형식: 글자
- 어떻게 셌나: 괄호가 든 값 검색

### 2. 예외

**예외 1 - 이름에 배송 메모가 붙음 (`김규준(직접수령)` `전주영(경비실)`)**

- 몇 건: 100건
- 어떻게 찾았나: 괄호 포함 값 검색

### 3. 처리

**예외 1 - 메모 혼입**

- 분류: 보류 (태그만. 분리는 안 함)
- 왜 이렇게 정했나: 메모는 배송에 필요한 정보라 지우면 아깝고, 분리하려면 "메모 칸"을 만들지부터 정해야 한다
- 왜 생겼다고 보는가: 배송 요청사항을 쓸 칸이 없어서 이름 칸에 적은 것
- 확신도: 추천 (별도 칸으로 분리하는 쪽)

### 4. 앞으로 어디서 막나

- 층: 정책
- 어떻게: 배송 요청사항 칸을 만들지, 만들면 선택식(경비실/문앞/직접수령)으로 할지 정한다

### 5. 못 정한 것 - 누구에게

- 요청사항 칸 신설 여부 → 내일 화면 설계에서 같이 결정

---

## 칸 - `weight_kg` `width_cm` `height_cm` `depth_cm` (무게와 치수, 묶어서 조사)

### 1. 초기 상태

- 값 종류: 숫자가 대부분, 일부에 단위가 붙음
- 빈 칸: 0건
- 형식: 섞임 (숫자 + `11.6kg` `32cm` 같은 글자)
- 어떻게 셌나: 숫자로 변환 안 되는 값 골라내기 + 범위 검사

### 2. 예외

**예외 1 - 단위 붙은 값 (`11.6kg`, `32cm`)**

- 몇 건: 무게 51건, 가로 46건
- 어떻게 찾았나: 숫자 변환 실패 값

**예외 2 - 말이 안 되는 값 (음수, 0, `999`kg, `9999`cm)**

- 몇 건: 무게 이상 48건(음수·0 37 + 극단값 11), 치수 이상 56건
- 어떻게 찾았나: 범위 검사 (무게 0~100kg, 치수 0~300cm 밖)

### 3. 처리

**예외 1 - 단위 붙음**

- 분류: 보류 (계산할 때만 단위를 떼고 읽음, 원본 유지)
- 왜 이렇게 정했나: 단위 떼기는 안전하지만 원본 보존 원칙 유지. 검산 계산에는 지장 없게 처리
- 왜 생겼다고 보는가: 손으로 치는 입력이라 단위까지 적음
- 확신도: 확정

**예외 2 - 이상값**

- 분류: 보류 (판정 칸에 `치수이상-계산불가` 등으로 표시)
- 왜 이렇게 정했나: 진짜 무게·크기는 상자를 다시 재지 않는 한 알 수 없다. 추정해 넣으면 요금까지 연쇄로 틀린다
- 왜 생겼다고 보는가: 입력 제한이 없어 오타(9999)나 임시값(-10)이 그대로 저장됨
- 확신도: 확정 (보류가 맞다)

### 4. 앞으로 어디서 막나

- 층: 시스템
- 어떻게: 숫자만 입력(단위는 화면이 표시), 범위 제한(치수 1~300cm 등). 장기적으로는 저울·측정기 연동으로 사람 입력 자체를 없앤다

### 5. 못 정한 것 - 누구에게

- 이상값 104건의 실제 무게·치수 → 복원 불가. 요금 정정이 필요한지만 정산 담당과 확인

---

## 칸 - `volume_weight_kg` `billed_weight_kg` (부피 무게와 청구 무게, 묶어서 검산)

### 1. 초기 상태

- 값 종류: 숫자 (부피무게 빈 칸 87건, 청구무게에 단위 붙은 2건)
- 형식: 숫자
- 어떻게 셌나: 공식으로 재계산해서 기재값과 대조 (계산값은 새 칸에, 원본 무수정)
  - 부피무게 = 가로x세로x높이/6000, 청구무게 = max(실제무게, 부피무게)

### 2. 예외

**예외 1 - 부피무게가 계산과 다르게 보였던 55건**

- 몇 건: 0건으로 판명
- 어떻게 찾았나: 치수가 정상인 행만 골라 재계산하니 2,903행 전부 일치. 어긋나 보였던 것은 치수 칸이 이상한 행이었음 (원인이 다른 칸에 있었다)

**예외 2 - 청구무게가 규칙과 다름**

- 몇 건: 62건 (무게·치수가 정상인데도 max 규칙과 불일치)
- 어떻게 찾았나: 재계산값과 비교, 판정 칸 `bw_check`

**예외 3 - 부피무게 빈 칸**

- 몇 건: 87건 (치수 정상인 86건은 계산값을 참고 칸에 채움)

### 3. 처리

**예외 2 - 청구무게 불일치 62건**

- 분류: 보류 (계산값을 `calc_billed_weight_kg`에 놓고 판정만)
- 왜 이렇게 정했나: 기재값이 틀렸는지, 우리가 모르는 예외 규칙(할증 등)이 있는지 아직 모른다
- 왜 생겼다고 보는가: 청구무게를 수기로 적으면서 비교를 빠뜨린 듯
- 확신도: 추천 (요금 규정으로 검산하면 확정)

### 4. 앞으로 어디서 막나

- 층: 시스템
- 어떻게: 부피무게·청구무게는 입력 칸에서 없앤다. 치수·무게를 넣으면 자동 계산

### 5. 못 정한 것 - 누구에게

- 62건이 단순 오기인지 할증 규칙인지 → 요금 규정(`02_접수규정.md`) + 정산 담당

---

## 칸 - `price` (요금)

### 1. 초기 상태

- 값 종류: 17가지 (빈 칸 포함, 음수 2종 포함)
- 빈 칸: 215건
- 형식: 숫자
- 어떻게 셌나: 값별 건수 + 같은 조건(크기 등급 x 권역)끼리 묶어 다수가 내는 요금과 비교

### 2. 예외

**예외 1 - 빈 칸**

- 몇 건: 215건

**예외 2 - 음수 요금 (`-1000`, `-1500`)**

- 몇 건: 4건

**예외 3 - 같은 조건인데 혼자 다른 요금**

- 몇 건: 49건
- 어떻게 찾았나: 같은 (등급 x 권역) 무리에서 90% 이상이 내는 요금이 있으면 그걸 기준으로 삼음. 데이터 다수결로 보면 소형-일반 4,000 / 중형-일반 6,000 / 대형-일반 9,000 / 중형-제주 9,000

### 3. 처리

**예외 1 - 빈 칸 215건**

- 분류: 보류
- 왜 이렇게 정했나: 안 받은 돈인지 안 적은 것인지 모른다. 0으로 채우면 못 받은 돈이 받은 것이 된다
- 확신도: 확정

**예외 2 - 음수 4건**

- 분류: 보류
- 왜 이렇게 정했나: 지우면 그 건의 흔적이 사라진다
- 왜 생겼다고 보는가: 환불을 요금 칸에 마이너스로 적은 듯
- 확신도: 추천

**예외 3 - 조건 대비 다른 요금 49건**

- 분류: 보류 (태그 `요금-같은조건다수와다름`)
- 왜 이렇게 정했나: 공식 요금표 없이 데이터 다수결로만 판단한 것이라, 할인·할증이면 정상일 수 있다
- 왜 생겼다고 보는가: 수기 계산 실수 또는 기록 안 된 할인
- 확신도: 추천 (요금표 오면 검산으로 확정)

### 4. 앞으로 어디서 막나

**예외 1·3 - 빈 칸, 계산 오류**

- 층: 시스템
- 어떻게: 요금은 입력하지 않는다. 무게·치수·지역에서 자동 계산하고, 계산이 안 되면 저장을 막는다

**예외 2 - 음수(환불)**

- 층: 정책
- 어떻게: 환불을 요금 칸에 적을지 별도 칸을 둘지 정한다

### 5. 못 정한 것 - 누구에게

- 빈 215건이 미수금인지 기록 누락인지 → 접수 담당자
- 환불 표기 방식 → 정산 담당

---

## 칸 - `eta_date` (도착 예정일)

### 1. 초기 상태

- 값 종류: 날짜 (모양이 3~4가지로 섞임)
- 빈 칸: 70건
- 형식: 섞임 (`2026-06-25` / `2026.06.25` / `2026/06/24`)
- 어떻게 셌나: 접수일부터 예정일까지 일수를 계산해 권역별 분포 확인 (계산 칸 `calc_eta_days`, 판정 칸 `eta_check`)

### 2. 예외

**예외 1 - 형식이 다른 표기**

- 몇 건: 91건 (날짜 자체는 전부 해석 가능)

**예외 2 - 배·비행기 지역인데 다음날 도착 예정**

- 몇 건: 36건 (제주 16, 도서산간 20)
- 어떻게 찾았나: 권역별 일수 분포. 정상 분포는 일반 1~3일, 제주 2~4일, 도서산간 3~5일인데 여기서 벗어남

**예외 3 - 빈 칸**

- 몇 건: 70건

### 3. 처리

**예외 2 - 하루 도착 의심 36건**

- 분류: 보류 (판정 표시만)
- 왜 이렇게 정했나: 권역별 소요일이 규정으로 며칠인지 몰라서 오류라고 확정할 수 없다
- 왜 생겼다고 보는가: 예정일을 자동 계산이 아니라 사람이 적는 듯. 권역을 안 보고 일반 기준(다음날)으로 적은 것
- 확신도: 추천 (규정의 권역별 일수가 오면 확정)

**예외 1·3 - 형식·빈 칸**

- 분류: 보류
- 확신도: 확정 (예정일 규칙이 정해지면 자동 계산으로 한 번에 해결될 칸)

### 4. 앞으로 어디서 막나

- 층: 시스템
- 어떻게: 예정일은 입력하지 않는다. 접수일 + 권역별 소요일로 자동 계산

### 5. 못 정한 것 - 누구에게

- 권역별 소요일 기준 → 접수 규정(`02_접수규정.md`) / 택배 회사

---

## 칸 - `accepted_at` `delivered_at` (접수 시각과 완료 시각, 묶어서 조사)

### 1. 초기 상태

- 값 종류: 날짜+시각 (접수 시각은 모양이 여러 가지)
- 빈 칸: 완료 시각 322건 (미완료 건이면 비는 게 정상)
- 형식: 섞임 (`2026-05-06 16:29:00` / `2026/05/21 15:47` / `2026.06.12 16시21분` / `26-05-19` / 월·일이 뒤집힌 `05/25/2026`)
- 어떻게 셌나: 표준 모양 검사 + 두 시각의 앞뒤 비교 + 상태와 대조

### 2. 예외

**예외 1 - 접수 시각 형식 제각각**

- 몇 건: 289건 (그중 완전히 다른 형식 181건, 미국식 월/일 반전 포함)

**예외 2 - 시간 역전 (완료가 접수보다 빠름)**

- 몇 건: 39건
- 어떻게 찾았나: 두 시각 비교. 물리적으로 불가능

**예외 3 - 상태와 완료 시각이 어긋남**

- 몇 건: 135건 (배송완료인데 시각 없음 65 + 미완료인데 시각 있음 70)
- 어떻게 찾았나: `status`와 교차 검사

### 3. 처리

**예외 1 - 형식**

- 분류: 보류 (계산할 때만 해석, 원본 유지)
- 왜 이렇게 정했나: `05/25/2026` 같은 미국식은 월·일이 둘 다 12 이하인 날짜에서는 어느 쪽인지 확정할 수 없다. 일괄 변환하면 날짜가 통째로 틀어질 수 있다
- 왜 생겼다고 보는가: 자유 입력 또는 여러 시스템의 기록 형식이 섞임
- 확신도: 추천 (모호하지 않은 건만 먼저 통일하는 방안)

**예외 2 - 시간 역전 39건**

- 분류: 보류 (태그)
- 왜 이렇게 정했나: 접수와 완료 중 어느 쪽이 틀렸는지 이 파일만으로는 모른다
- 확신도: 추천 (배송 추적 기록의 단계별 시각과 대조하면 판별 가능)

**예외 3 - 상태-시각 불일치 135건**

- 분류: 보류 (태그)
- 왜 이렇게 정했나: 상태가 늦게 갱신된 건지 시각이 잘못 든 건지 모른다
- 확신도: 추천 (추적 기록과 대조)

### 4. 앞으로 어디서 막나

- 층: 시스템
- 어떻게: 시각은 사람이 치지 않는다. 접수·배송 처리 시점에 시스템이 자동으로 찍는다. 상태와 시각을 한 동작으로 같이 기록해 어긋남 자체를 없앤다

### 5. 못 정한 것 - 누구에게

- 시간 역전 건의 어느 시각이 맞는지 → `tracking_events.csv` 대조 / 시스템 관리자

---

# 마무리

## 검산

**칸 - `receiver_area`**

- 처리 전: 25가지 표기
- 처리 후: 18가지 (통일 245칸, `도서산간` 19건은 보류로 남김)
- 예상과 달랐던 것: 동네(`receiver_dong`)와 지역이 어긋난 행이 있을 줄 알았는데 0건. 표기만 갈렸지 내용은 맞았다

**칸 - `volume_weight_kg`**

- 처리 전: 계산과 안 맞아 보이는 행 55건
- 처리 후: 치수 정상 행 2,903건 전부 공식과 일치. "부피무게 오류"가 아니라 "치수 오류"였다
- 예상과 달랐던 것: 의심했던 칸이 무죄고 원인이 옆 칸에 있었다. 칸 하나만 보면 틀리게 판단할 뻔했다

**정답 파일과 대조 (verified_300.csv)**

- 어디까지: 아직 못 함
- 왜 멈췄나: 정답 파일이 아직 전달되지 않음. 파일과 검산 양식이 오면 운송장 번호로 연결해 바로 채점 예정

## 동료와 대 보기

- 나의 판단: (위 칸별 정책)
- 다른 사람 판단: 아직 대조 전
- 같았나: 5시 검산 시간에 확인 예정

## 원본 보존

- [x] 원래 칸은 그대로 두고 새 칸을 옆에 만들었다
- [x] 원래 칸을 덮어썼다 (표기 통일 440칸만)

새로 만든 칸:

- `dup_flag` - 중복 표시
- `fix_note` - 표기 통일할 때 바꾼 내역 (원값이 여기 남아 있음)
- `hold_flag` - 보류 표시
- `calc_volume_weight_kg` / `vw_check` - 부피무게 재계산값과 판정
- `calc_billed_weight_kg` / `bw_check` - 청구무게 재계산값과 판정
- `calc_eta_days` / `eta_check` - 예정일 일수와 판정
- `pattern_check` - 앞뒤 안 맞는 것 분류 태그

원래 칸을 덮어썼다면 되돌릴 방법:

- 표기 통일 440칸은 `fix_note`에 "칸:원값→새값"이 전부 기록돼 있어 역방향으로 되돌릴 수 있음. 원본 파일도 별도 보관 중

## 이번에 다루지 않기로 한 것

- `item_name`(물품명): 고객이 자유롭게 적는 값이라 정답이 없다. 검증 대상이 아니라고 판단
- 크기 등급의 경계 검증(이 무게면 이 등급이 맞는지): 등급표(규정) 없이 데이터만으로 추측하면 섣부른 판단이 됨
- 지점별 미배송 비율 분석: 데이터 오류가 아니라 운영 분석이라 오늘 범위 밖

## 다음 사람에게

- 파일은 `shipments_정리본_v5.csv` 하나만 보면 된다 (v1~v4는 중간 단계)
- 행은 하나도 안 지웠다. 3,045행 그대로. 지울 후보는 `dup_flag`, 판단 보류는 `hold_flag`, 이상 패턴은 `pattern_check`를 보면 된다
- 다음에 올 파일 3개가 각각 여는 것: `verified_300.csv`(채점) / `02_접수규정.md`(요금·등급·예정일 검산 확정) / `tracking_events.csv`(중복 판별, 시간 역전 판별)
- 작업 이력은 `todo-list-iny.md`에 처리 방식별로 정리돼 있다

## 막힌 자리

- `배송실패` `반송` `회수` `배송중` `배달중`의 정확한 뜻 (현장 확인 필요)
- 지역칸 `도서산간` 13건의 원래 지역 (원천 주소 없이는 복원 불가)
- 이상 무게·치수 104건의 실제 값 (다시 잴 수 없음)

---

## 내일 미리 보기 - 4번(어디서 막나) 요약

- **시스템으로 표시한 것** (내일 화면에서 구현): 운송장 번호 자동 발급, 상태 자동 기록,
  등급·부피무게·청구무게·요금·예정일 자동 계산, 지역 선택 목록, 전화번호 자동 형식+자릿수 검사,
  치수·무게 숫자만+범위 제한, 접수 경로 자동 기록, 시각 자동 기록
- **정책으로 남긴 것** (협의 대상): 상태 체계 몇 가지로 할지, 규격 밖 화물 받을지,
  지역 목록의 단위, 환불 표기 방식, 배송 요청사항 칸 신설, 전화 없는 접수 허용 여부
- **운영으로 뺀 것**: 규격 밖 화물 상담원 연결, 전화 없는 접수 예외 처리, 미수금 확인
