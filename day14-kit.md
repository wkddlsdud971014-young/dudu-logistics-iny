# Day14 실습 키트 - 목요일 (표준 절차 1-3단계)

---

## 이틀 브리핑 - 목요일과 금요일에 무엇을 하나

오늘부터 이틀 동안 **두두택배라는 회사의 접수 시스템**을 맡습니다. 실제 컨설팅이 시작될 때처럼, 지금까지 그 회사가 쓰던 문서와 데이터를 통째로 받았습니다.

이틀을 이렇게 씁니다.

| | 목요일 (오늘) | 금요일 (내일) |
|---|---|---|
| **하는 일** | 조사한다 | 만든다 |
| 오전 | 택배업이 어떻게 돌아가는지 + 데이터 열어 보기 | 접수 화면 만들기 |
| 오후 | 칸마다 믿을 수 있는지 조사 | 정답과 대 보기 |
| 오후 2부 | **못 믿는 값을 어떻게 할지 정하기** | 배포하고 기록 남기기 |
| 손에 남는 것 | 내 처리 방침 | 접수 1건이 도는 화면 |

**오늘은 코드를 한 줄도 쓰지 않습니다.** 이상하게 들리겠지만 이유가 있습니다.

데이터를 안 보고 만들면 이런 일이 생깁니다. 만들고 보니 그 칸이 절반은 비어 있었다. 지역 이름이 세 가지로 적혀 있어 집계가 안 맞는다. 요금 계산을 다 만들었는데 맞는지 확인할 방법이 없다.

그래서 오늘 조사하고, 조사한 것으로 규칙을 정하고, **내일 그 규칙을 그대로 기능으로 만듭니다.** 오늘 정한 규칙이 내일의 설계도입니다.

**이번 주에 시스템을 다 만들지 않습니다.** 접수 목록도, 상세 조회도, 관리자 화면도 만들지 않습니다. 목표는 완성이 아니라 **절차를 한 번 끝까지 밟아 보는 것**입니다. 절차 전체는 `00_표준절차_6단계.md`에 있습니다.

**정답이 없는 실습입니다.** 숫자를 틀리게 세도, 절반만 해도, 막혀서 멈춰도 됩니다. 해 보고 나온 것을 적으면 됩니다.

---

## 0. 아침 확인 (09:00)

1. 어제 배포 주소를 엽니다 - 열리는지 확인
2. 내 GitHub 저장소를 엽니다 - 커밋이 쌓여 있는지 확인
3. #실습공유에 "어제 것 열림" 한 줄

받은 자료(`두두택배_인수인계` 폴더)를 **내 프로젝트 폴더 안으로 통째로** 옮깁니다. 폴더 이름은 그대로 둡니다.

폴더 안에 있어야 하는 이유: 에이전트는 프로젝트 폴더 밖의 파일을 읽지 못합니다. 오늘은 안 쓰지만 내일 규정 문서를 읽혀야 합니다.

---

# 오전 - 도메인 알기 + 실습 1

## 강의 (30분)

### 택배업이 어떻게 돌아가는가 (20분)

`04_택배업_기본지식.md`를 함께 봅니다. 여섯 가지입니다.

1. 왜 물건이 곧장 안 가고 허브를 거치는가
2. 왜 이불이 쌀보다 비싼가 (부피 무게)
3. 왜 배송이 실패하는가
4. 운송장 번호는 왜 필요한가
5. 접수한 곳과 도착할 곳은 다르다
6. 권역이 무엇인가

**마지막 표를 손에 들고 있습니다.** 방금 들은 것이 데이터의 어느 칸인지, **그 칸이 몇 가지여야 하는지**가 적혀 있습니다. 오늘 실습은 전부 이 표에서 출발합니다.

### 원장 훑기 (10분)

`01_사업현황_인수인계.md`에서 **0절(부탁하는 것)과 4절(우리가 아는 문제)** 두 곳만 봅니다. 전부 읽지 않습니다.

세 가지에 답할 수 있으면 됩니다. 답을 내 실습 스레드에 한 줄로 올립니다.

1. 두두택배가 오늘 부탁한 것은 무엇입니까
2. 지금 이 회사는 무엇으로 접수 기록을 관리하고 있습니까
3. 접수하면 안 되는 물건은 어느 문서에 적혀 있습니까

---

## 실습 1 - 원장 1차 조사

**채울 파일**: `실습파일/실습1_조사_1차.md` - 미리 만들어 둔 양식입니다. VS Code에서 열어 두고 채워 갑니다.

### 1-1. 크기와 생김새 (Claude Code에 입력)

```text
두두택배_인수인계/data/shipments.csv를 읽고, 몇 행 몇 열인지 알려줘. 그리고 각 열에 어떤 값이 들어 있는지 예시를 두세 개씩 보여줘.
```

### 1-2. 값 종류 세기 (Claude Code에 입력)

```text
같은 파일에서 receiver_area, region_type, size_grade, status, channel, branch_name 이 여섯 칸의 값 종류를 전부 나열해줘. 각 종류가 몇 건인지도 같이.
```

### 1-3. 기본지식 표와 대조하기

`실습1_조사_1차.md`의 **2번 표**를 채웁니다. `04_택배업_기본지식.md` 마지막 표가 왼쪽 열의 근거입니다.

**차이가 나는 칸이 오후에 조사할 곳입니다.**

### 1-4. 회사가 궁금해한 것 하나 답해 보기 (Claude Code에 입력)

인수인계 문서에 두두택배가 궁금해한 것이 세 개 있습니다. 그중 하나만 답해 봅니다.

```text
두두택배_인수인계/data/shipments.csv에서 운송장 번호 앞 두 자리가 접수 지점 코드야.
지점별로 전체 건수와 미배송 건수, 그리고 미배송 비율을 높은 순으로 알려줘. 숫자만.
```

나온 결과를 보고 한 줄 적습니다: **"OO지점이 유독 높다 / 지점 간 차이가 별로 없다"**

이게 데이터로 답을 만든 첫 경험입니다. 눈으로는 3,045행에서 절대 볼 수 없는 것입니다.

### 1-5. 양식 채우기 (Claude Code에 입력)

```text
실습파일/실습1_조사_1차.md를 열고, 지금까지 조사한 숫자를 빈 칸에 채워줘.
숫자만 넣고 해석은 넣지 마. 4번 "내가 본 것"은 비워 둬.
```

**4번은 내가 직접 씁니다.** 에이전트가 채우면 그건 에이전트가 본 것이지 내가 본 것이 아닙니다.

### 1-6. 10분 메모

**다음 10분: (행동)해서 (눈에 보이는 결과)가 나오면 완료**

---

# 오후 - 실습 2: 칸별 조사

**채울 파일**: `실습파일/실습2_판정표.md`

오전에 "몇 가지여야 하는데 다르다"를 봤습니다. 이제 칸 하나씩 "이걸 그대로 써도 되나"를 묻습니다. 칸마다 네 가지를 봅니다.

1. 비어 있는 건이 몇 건인가
2. 같은 것을 다르게 적은 것이 있나
3. 값이 말이 안 되는 건이 있나
4. 다른 칸과 어긋나는 건이 있나

### 2-1. 먼저 세 칸만 (Claude Code에 입력)

한 번에 다 시키면 에이전트가 일부를 빠뜨립니다. 세 칸을 먼저 하고 확인합니다.

```text
두두택배_인수인계/data/shipments.csv에서 아래 세 칸을 조사해줘. 칸마다 (1) 비어 있는 건수 (2) 값 종류 (3) 말이 안 되는 값이 있으면 몇 건인지, 숫자로만 알려줘. 추측은 넣지 마.
- price (요금)
- weight_kg (실제 무게)
- receiver_area (도착 지역)
```

### 2-2. 나머지 칸 (Claude Code에 입력)

```text
같은 방식으로 tracking_no, receiver_phone, delivered_at, status, size_grade, item_name, eta_date, region_type 도 조사해줘. 칸별로 표로 정리해줘.
```

### 2-3. 칸끼리 어긋나는 것 (Claude Code에 입력)

한 칸만 봐서는 안 보이는 문제가 있습니다.

```text
아래 세 가지가 각각 몇 건인지 세어줘.
1) status가 배송완료인데 delivered_at이 비어 있는 건
2) delivered_at이 accepted_at보다 빠른 건
3) 같은 tracking_no가 두 줄 이상인 건
```

### 2-4. 숫자를 양식에 채우기 (Claude Code에 입력)

```text
실습파일/실습2_판정표.md를 열고, 지금까지 조사한 숫자를 표에 채워줘.
"내 판정" 칸은 비워 둬. 그건 내가 정할 거야.
```

### 2-5. 판정은 내가 붙입니다

`실습2_판정표.md`의 **내 판정** 칸을 직접 채웁니다. 셋 중 하나입니다.

- **그대로** - 이 칸은 믿고 쓸 수 있다
- **손본다** - 값을 고치거나 통일하면 쓸 수 있다
- **못 쓴다** - 이 칸은 이번에 안 쓴다

몇 칸을 채우든 상관없습니다. **판정은 내가 정하는 것이라 맞고 틀림이 없습니다.** 세 칸에서 막혔으면 세 칸까지 적고 막힌 자리를 씁니다.

마지막으로 **가장 심각해 보이는 것 세 개**를 고릅니다. 이게 오후 2부의 재료입니다.

### 2-6. 10분 메모

---

# 오후 2부 (개별) - 실습 3: 처리 방침

**채울 파일**: `실습파일/실습3_내_규칙.md` -> 그다음 `CLAUDE.md`에 옮김

여기가 오늘의 핵심입니다. 각자 진행합니다.

### 3-1. 처리 방침 정하기 - 손으로 씁니다

`실습파일/실습3_내_규칙.md`를 열고 표를 **직접** 채웁니다. 여기는 에이전트를 쓰지 않습니다.

실습 2에서 고른 "가장 심각해 보이는 것 세 개"가 출발점입니다. 거기서 **몇 개만** 고릅니다. 다섯 개가 목표지만 한 개여도 됩니다.

각 줄이 **내일 만들 기능 하나와 짝지어져야** 합니다. 짝이 안 지어지면 아직 규칙이 아니라 관찰입니다.

파일 안에 참고 예시가 접혀 있습니다. 펼쳐서 그대로 베껴도 되고 전혀 다르게 써도 됩니다.

### 3-2. 다루지 않을 것도 적습니다

같은 파일의 "이번에 다루지 않을 것"을 채웁니다. 안 하기로 정하는 것도 산출물입니다. 하나여도 됩니다.

### 3-3. CLAUDE.md로 옮깁니다 (Claude Code에 입력)

```text
실습파일/실습3_내_규칙.md를 읽고, 거기 적힌 규칙을 CLAUDE.md 맨 아래 "내 규칙" 부분에 추가해줘.
각 규칙 아래에 그 규칙 때문에 만들어야 하는 기능도 한 줄씩 같이 적어줘.
```

내일 이 규칙을 그대로 기능으로 만듭니다.

**내일 이렇게 됩니다.** 오늘 "지역은 정해진 것 중에서만 고른다"라고 적었으면, 내일 접수 화면의 지역 칸이 자유 입력이 아니라 선택 목록이 됩니다. "무게 0은 접수 불가"라고 적었으면 0을 넣었을 때 막히는 화면이 됩니다.

규칙이 애매하면 내일 헤매게 되는데, 그 헤맴 자체가 내일의 관찰거리입니다. 지금 완벽하게 쓰려고 오래 붙들지 마십시오.

---

## 4. 제출 (오늘 23:59)

과제 공지를 보십시오. 오늘은 **배포 주소가 없습니다** - 만든 것이 없으니 당연합니다.

**오늘 채운 세 파일이 그대로 제출물입니다.**

| 파일 | 언제 채웠나 |
|---|---|
| `실습파일/실습1_조사_1차.md` | 오전 |
| `실습파일/실습2_판정표.md` | 오후 |
| `실습파일/실습3_내_규칙.md` | 오후 2부 |

세 파일의 내용을 제출 양식(`day14_제출양식.md`)에 옮겨 담고 `day14_이름.md`로 업로드합니다. 빈 칸이 있어도 냅니다.

## 5. 내일 준비

내일 아침에 필요한 것은 두 가지뿐입니다.
1. `CLAUDE.md`에 들어간 내 규칙
2. `두두택배_인수인계` 폴더가 내 프로젝트 안에 있을 것

이 둘만 있으면 내일 바로 시작합니다.
