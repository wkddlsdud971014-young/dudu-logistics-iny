# 화면에서 막을 것 / 서버에서 막을 것

- 작성자: 장인영
- 처음 작성 2026-08-07 · 화면을 고친 뒤 다시 잼 · **표를 붙인 뒤 최종 확인 2026-08-07**
- 왜 만들었나: 정책서에서 층을 `시스템`이라고만 적은 12칸이, **화면에서 막는 것인지 서버에서 막는 것인지** 갈리지 않았다. 갈리지 않으면 무엇을 어디에 만들지 정할 수 없다. (피드백 지적 사항)

---

## 먼저, 화면과 서버가 뭐가 다른가

가게로 치면 이렇다.

| | 화면 | 서버 |
|---|---|---|
| 누구냐 | **가게 앞에 서 있는 직원** | **본사에 하나뿐인 장부** |
| 하는 일 | 손님이 잘못 적으면 그 자리에서 알려 준다 | 적힌 것을 실제로 받아 적는다 |
| 못 하는 일 | **옆 가게에서 무슨 일이 있는지 모른다** | 손님에게 미리 알려 주지 못한다 |
| 빠르기 | 즉시 | 저장할 때 한 번 |

**둘 다 필요하다.** 화면만 있으면 옆 가게와 부딪히고, 서버만 있으면 손님이 다 적고 나서야 "안 됩니다" 소리를 듣는다.

### 가르는 법은 질문 하나

> **"가게 두 곳에서 똑같이 동시에 하면 뚫리는가?"**

**뚫리면 서버**에서 막아야 한다. 화면은 가게마다 따로 돌아서 서로를 모르기 때문이다.

**예를 들어 운송장 번호.**
서울지점 창구가 두 개인데, 두 손님이 같은 순간에 접수했다고 하자.

```
창구 A 화면: "지금까지 1번까지 나갔네. 그럼 2번!"   →  1100000002
창구 B 화면: "지금까지 1번까지 나갔네. 그럼 2번!"   →  1100000002
```

**둘 다 2번을 뽑는다.** 서로를 모르니까. 정책서에서 찾으신 **중복 45쌍**이 이렇게 생겼을 가능성이 크다.

본사 장부(서버)에 **"이 번호는 한 번만 받는다"** 고 걸어 두면, 뒤에 온 쪽이 튕겨 나가고 3번을 다시 받는다. 이건 화면을 아무리 잘 만들어도 못 막는다.

---

## 결론 먼저: 서버에서만 되는 것 3가지

| 무엇 | 화면으로는 왜 안 되나 | 서버에 넣을 것 |
|---|---|---|
| **운송장 번호 겹침** | 창구 두 곳이 서로를 모른다 | 번호 칸에 `unique` (한 번만 받기) |
| **접수 시각** | 손님 컴퓨터 시계는 바꿀 수 있다 | 저장할 때 `now()` (본사 시계로 찍기) |
| **접수 직후 상태** | 사람이 고를 값이 아니다 | 기본값 `집화처리` |

**나머지는 전부 화면에서 막는다.**

---

## 최종 확인 (표를 붙인 뒤 배포된 화면에서 직접 잼)

`○` 됨 · `△` 반만 · `✕` 안 됨 · `—` 오늘 범위 밖

| # | 정책서 칸 | 규칙 | 어디서 막나 | 결과 | 무엇으로 확인했나 |
|---|---|---|---|---|---|
| 1 | `tracking_no` | 겹치는 번호 거부 | **서버** | ○ | 같은 번호 두 번 넣어 **409** 받음 |
| 2 | `status` | 상태는 안 적는다 | **서버** | ○ | 손님이 보낸 값 무시하고 `집화처리` |
| 3 | `status` (뜻 모를 값) | 상태 체계 정하기 | 정책 | — | 현장 확인 필요 |
| 4 | `size_grade` | 자동 계산 | 화면 | ○ | `determineGrade` |
| 5 | `size_grade` (규격 밖) | 못 받으면 안내 | 화면 | ○ | 상자 나누라고 안내 |
| 6 | `channel` | 기기가 자동 기록 | 화면 | ○ | 창구라 `방문접수` 고정 |
| 7 | `receiver_area` | 자유 입력 없애기 | 화면 | ○ | 버튼 22곳 + 시·군·구 230개 + 동 84개 |
| 8 | `receiver_area` (권역 혼입) | 권역 자동 | 화면 | ○ | 사람이 못 고침 |
| 9 | `receiver_phone` | 자동 형식 | 화면 | ○ | 숫자만 치면 하이픈 |
| 10 | `receiver_phone` (빈칸) | 필수 | 화면 | ○ | 자릿수까지 검사 |
| 11 | `receiver_name` (메모 섞임) | 요청사항 칸 만들지 정하기 | 정책→화면 | ○ | **버튼 5개로 만듦** + 이름칸 괄호 차단 |
| 12 | 무게·치수 4칸 | 범위 제한 | 화면 | ○ | 무게 0.1~100 / 치수 1~300 |
| 13 | `volume_weight_kg` | 자동 계산 | 화면 | ○ | 저장까지 됨 |
| 14 | `billed_weight_kg` | 자동 계산 | 화면 | ○ | |
| 15 | `price` | 자동, 안 되면 저장 막기 | 화면+서버 | ○ | 버튼 잠김 |
| 16 | `price` (음수 환불) | 환불 적는 법 정하기 | 정책 | — | 정산 담당 확인 필요 |
| 17 | `eta_date` | 접수일+권역 자동 | 화면 | ○ | 주말 빼고 셈 |
| 18 | `accepted_at` | 시스템이 찍기 | **서버** | ○ | 서버 시계로 `2026-08-07T04:27:16` |

**된 것 16 · 오늘 범위 밖 2 · 안 된 것 0**

### 서버에서만 되는 것 3가지 — 전부 확인됨

| 무엇 | 어떻게 확인했나 |
|---|---|
| 운송장 번호 겹침 | 같은 번호를 두 번 넣었더니 **HTTP 409** 로 튕김 |
| 접수 시각 | `"acceptedAt":"2000-01-01"` 을 일부러 보냈는데 **무시하고 서버 시계** |
| 접수 직후 상태 | `"status":"손님이보낸값"` 을 보냈는데 **무시하고 `집화처리`** |

서버가 이 세 칸은 손님이 보낸 값을 **아예 안 받게** 막아 두었다(`SERVER_ONLY`). 화면을 우회해도 못 바꾼다.

## 저장할 때 담기는 칸 (원장 25칸 중 22칸)

실제로 표에 들어간 것을 조회해서 셌다.

**담기는 22칸**

```
운송장 · 접수시각 · 지점 · 보내는분 이름/전화 · 받는분 이름/전화
지역 · 상세주소 · 권역 · 물품종류 · 물품명 · 무게 · 가로 · 세로 · 높이
부피무게 · 청구무게 · 등급 · 요금 · 도착예정일 · 상태 · 경로
```

**안 담기는 3칸과 그 이유**

| 칸 | 왜 없나 |
|---|---|
| `branch_code` | 지점 이름을 담고 코드는 거기서 뽑는다. 운송장 앞 2자리로도 남는다 |
| `sender_id` | 고객 명단(1,200명)과 이어 붙이는 일. **오늘 범위 밖** |
| `delivered_at` | 배송이 끝나야 생기는 값. 접수 화면이 만들 수 없다 |

## 화면이 못 하는 일 하나 더 (2026-08-07 확인)

**도착 지역 검색**은 세 겹으로 되어 있다.

| 무엇으로 찾나 | 되나 | 어디서 왔나 |
|---|---|---|
| 시·도 (서울, 경기) | ○ | 규정 |
| 시·군·구 (중랑구, 분당, 해운대) | ○ 약 230개 | 내가 넣은 목록 |
| 동·읍·면 (노형동, 둔산동) | ○ 84개 | **우리 원장에 실제로 있는 값** |
| 번지·도로명 (테헤란로 152) | ✕ | 주소 검색 서비스가 필요. 열쇠를 받아야 함 |

전국 읍·면·동은 3,500개가 넘는데, 우리 원장에는 84개만 있다. 나머지를 **지어내서 적으면 틀린 값이 섞인다.** 그래서 넣지 않았다. 필요해지면 행정안전부 공개 목록을 받아 넣거나 주소 검색 서비스를 붙여야 한다.

---

## 이 표를 어디에 쓰나

1. **`setup.sql` 고칠 때** — 위 "서버" 3줄이 그대로 할 일이 된다
2. **자가 테스트할 때** — `○` 칸을 하나씩 다시 눌러 확인한다
3. **정책서 고칠 때** — 층을 `시스템` → `화면` / `서버` 로 바꿔 적는다
