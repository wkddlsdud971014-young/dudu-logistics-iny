# 화면 / 서버 대조표

- 작성자: 장인영
- 작성일: 2026-08-07
- 왜 만들었나: 정책서에서 층을 `시스템`으로만 적은 12칸이 **화면에서 막는 것인지 서버에서 막는 것인지** 갈리지 않아, 무엇을 어디에 만들지 정할 수 없었다. (피드백 지적 사항)
- 어떻게 채웠나: 배포된 화면(`index.html`)을 실제로 눌러보고, 코드에서 해당 칸·검사가 실재하는지 확인해 적었다. 추정으로 적은 칸은 없다.

## 가르는 기준

| 층 | 무엇을 뜻하나 | 왜 여기여야 하나 |
|---|---|---|
| **화면** | 브라우저에서 막는다 | 사람이 잘못 넣기 **전에** 막을 수 있다. 즉시 알려줄 수 있다 |
| **서버** | 저장하는 순간 막는다 | 화면을 우회해도, 창구 두 곳이 동시에 접수해도 막힌다 |
| **정책** | 사람이 규칙부터 정해야 한다 | 무엇이 맞는지 아직 아무도 모른다 |
| **운영** | 현장 절차로 뺀다 | 시스템이 판단할 수 없다 |

**핵심 판별법**: *"창구 두 곳에서 동시에 하면 뚫리는가?"* 뚫리면 **서버**다. 화면은 창구마다 따로 돌아서 서로를 모른다.

---

## 대조표 (17줄)

| # | 정책서 칸 | 정책서에 적은 규칙 | 정책서 층 | 화면에서 지금 도나 (실측) | **갈라야 할 층** |
|---|---|---|---|---|---|
| 1 | `tracking_no` | 번호는 시스템이 발급. 이미 있는 번호는 저장 자체를 거부 | 시스템 | ⚠️ 자동 발급은 되나, 중복 검사가 **브라우저 메모리 안에서만**. 새로고침하면 `1100000001`이 다시 발급됨 | **서버** |
| 2 | `status` 예외1 | 상태는 입력하지 않는다. 이벤트가 생기면 자동으로 바뀐다 | 시스템 | ❌ 상태 칸도, 저장값도 없음 | **서버** |
| 3 | `status` 예외2 | 상태 체계를 몇 가지로 할지 규칙부터 정한다 | 정책 | — | 정책 (오늘 범위 밖) |
| 4 | `size_grade` 예외1·3 | 등급은 입력받지 않는다. 무게·치수에서 자동 계산 | 시스템 | ✅ 자동 계산됨. 입력칸 없음 | **화면** (됨) + 서버 재계산 |
| 5 | `size_grade` 예외2 | 4등급 밖 화물은 접수 불가 안내 후 상담원 연결 | 정책+운영 | ⚠️ 대형 초과 차단은 되나 **상담원 연결 안내 없음** | 화면(차단) + **운영**(연결) |
| 6 | `channel` | 접수 경로는 사람이 적지 않는다. 접수한 기기가 자동 기록 | 시스템 | ❌ 칸도 저장값도 없음 | **화면** |
| 7 | `receiver_area` 예외1 | 자유 입력을 없애고 선택 목록으로 받는다 | 시스템 | ✅ **오늘 고침** — 22곳 선택 목록 | **화면** + 서버 재검사 |
| 8 | `receiver_area` 예외2 | 권역은 지역에서 자동 계산하고 사람이 적는 칸에서 없앤다 | 시스템 | ✅ **오늘 고침** — 권역 자동 표시, 사람이 못 고침 | **화면** |
| 9 | `receiver_phone` 예외1 | 자동으로 `010-0000-0000` 모양. 자릿수 안 맞으면 못 넘어감 | 시스템 | ❌ **칸 자체가 없음** | **화면** |
| 10 | `receiver_phone` 예외2 | 필수 입력으로 막되, 번호 없는 접수 허용은 운영이 정함 | 시스템+운영 | ❌ 칸 자체가 없음 | **화면**(필수) + 운영(예외) |
| 11 | `receiver_name` | 배송 요청사항 칸을 만들지, 만들면 선택식으로 할지 정한다 | 정책 | ❌ 요청사항 칸 없음. 이름칸 괄호도 안 막음 | **정책 → 오늘 결정** + 화면 |
| 12 | 무게·치수 4칸 | 숫자만 입력, 범위 제한 (치수 1~300cm 등) | 시스템 | ⚠️ 0 이하만 거부. **상한 없음** (`max` 속성 0개 — `9999cm` 통과) | **화면** + 서버 재검사 |
| 13 | `volume_weight_kg` | 부피무게는 입력칸에서 없앤다. 자동 계산 | 시스템 | ✅ 입력칸 없음, 자동 계산됨. ⚠️ 다만 **저장값에 안 담김** | **화면** + 서버 저장 |
| 14 | `billed_weight_kg` | 청구무게도 자동 계산 | 시스템 | ✅ 자동 계산·저장됨 | **화면** + 서버 재계산 |
| 15 | `price` 예외1·3 | 요금은 입력하지 않는다. 자동 계산하고 계산 안 되면 저장을 막는다 | 시스템 | ✅ 자동 계산, 계산 안 되면 버튼 잠김 | **화면** + **서버**(재계산해 다르면 거부) |
| 16 | `price` 예외2 | 환불을 요금 칸에 적을지 별도 칸을 둘지 정한다 | 정책 | — | 정책 (오늘 범위 밖) |
| 17 | `eta_date` | 예정일은 입력하지 않는다. 접수일 + 권역별 소요일로 자동 계산 | 시스템 | ✅ 영업일 계산됨 (주말 제외) | **화면** + 서버 재계산 |
| 18 | `accepted_at` | 시각은 사람이 치지 않는다. 시스템이 자동으로 찍는다 | 시스템 | ❌ **저장값에 접수 시각이 없음** | **서버** |

---

## 갈라 보니 이렇게 나뉜다

정책서에서 `시스템` 하나로 뭉쳐 있던 12칸이 셋으로 갈렸다.

### 서버에서만 막을 수 있는 것 — 3개 ⚠️ 화면으로는 절대 못 막는다

| 항목 | 왜 화면으로는 안 되나 | 무엇을 만드나 |
|---|---|---|
| 운송장 번호 겹침 | 창구 두 곳이 동시에 접수하면 서로를 모른다. 새로고침만 해도 번호가 되감긴다 | `setup.sql`에 `unique` 제약 |
| 접수 시각 | 브라우저 시계는 사용자가 바꿀 수 있다 | DB `default now()` |
| 접수 직후 상태 | 사람이 고를 값이 아니다 | DB `default '집화처리'` |

**이게 오늘 오전에 몰랐던 것이다.** 운송장 중복은 정책서에서 첫 번째로 다룬 문제인데, 화면만 고쳐서는 영원히 안 막힌다.

### 화면에서 막아야 하는 것 — 이미 된 것 6개 / 아직 안 된 것 4개

**된 것**: 지역 선택목록 · 권역 자동 · 등급 자동 · 부피/청구무게 자동 · 요금 자동 · 예정일 자동
**안 된 것**: 전화칸 신설 · 치수·무게 상한 · 접수경로 자동 기록 · 배송요청사항 칸

### 오늘 사람이 정해야 하는 것 — 1개

`receiver_name` 메모 혼입 100건 → **배송 요청사항 칸을 신설할지.**
정책서에 *"내일 화면 설계에서 같이 결정"* 이라고 미뤄둔 것이고, 오늘이 그 내일이다.
검산 결과(`result.md`)가 근거를 준다 — *"정답은 이름만 있다. 괄호 메모와 `님` 호칭을 분리하는 게 정답이었다."*
→ **분리하는 쪽으로 정한다. 선택식(문앞/경비실/직접수령/없음)으로 만든다.**

---

## 이 표를 어디에 쓰나

1. **13:00 `setup.sql`** — 위 "서버" 3줄이 그대로 할 일이 된다
2. **14:00 자가 테스트** — 이 표의 "화면에서 지금 도나" 칸을 하나씩 다시 눌러 확인한다
3. **정책서 갱신** — 층을 `시스템` → `화면` / `서버`로 바꿔 적는다
